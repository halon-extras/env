[
    {
        "name": "getenv",
        "parameters": {
            "required": [
                {
                    "name": "name",
                    "type": "string"
                }
            ]
        },
        "returnType": "string or none",
        "detail": "getenv(string $name): string or none",
        "value": "getenv($0)",
        "documentation": "Get an environment variable as a string. If not found none is returned.",
        "link": "[Full documentation](https://github.com/halon-extras/env)"
    }
]
